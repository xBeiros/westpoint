<?php

/*
|--------------------------------------------------------------------------
| Authentication Routes
|--------------------------------------------------------------------------
|
| Diese Datei ist leer, da Laravel Fortify alle Authentifizierungs-Routen
| automatisch registriert. Die Routen werden über den FortifyServiceProvider
| verwaltet.
|
| Fortify registriert automatisch folgende Routen:
| - /login (GET, POST)
| - /register (GET, POST) - wenn aktiviert
| - /logout (POST)
| - /forgot-password (GET, POST)
| - /reset-password/{token} (GET, POST)
| - /email/verification-notification (POST)
| - /verify-email/{id}/{hash} (GET)
| - /two-factor-challenge (GET, POST)
| - /confirm-password (GET, POST)
| - /user/two-factor-authentication (POST, DELETE)
| - /user/confirmed-two-factor-authentication (POST)
| - /user/two-factor-qr-code (GET)
| - /user/two-factor-recovery-codes (GET)
| - /user/two-factor-recovery-codes (POST)
|
*/
